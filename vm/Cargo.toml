[package]
name = "vm"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.47", features = ["derive"] }

common = { path = "../common" }
runtime = { path = "../runtime" }
tracing-log = { workspace = true }
