[package]
name = "runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
once_cell = "1.19"
dashmap = "6.1.0"
zip = "5.0.0"
parking_lot = "0.12"

thiserror = { workspace = true }
tracing-log = { workspace = true }

jclass = { path = "../jclass" }
common = { path = "../common" }

# TODO to be deleted
toml = "0.8"
# TODO to be moved to the test scope
serde = { version = "1.0", features = ["derive"] }
log = "0.4.27"

[dev-dependencies]
insta = { version = "1.42.2", features = ["yaml"] }
rstest = "0.26.1"
